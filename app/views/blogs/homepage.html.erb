<div id="MENU_HEADER" class="pageTitle">
  Homepage Blogs
</div>
<div style="clear:both;"></div><hr/>

  <% if @blogs.empty? %>
    No Blogs Found!
  <% else %>
    <%= render :partial => 'blog', :collection => @blogs %>
  <% end %>
