<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
  <head>
    <meta http-equiv="Content-Type" content="text/html; utf-8" />
    <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon"/>
    <link rel="alternate" type="application/rss+xml"
          title="PMF Blogs Feed" href="<%= url_for :only_path => false, :controller => "blogs", :format => "rss" %>" />

    <meta name="description" content="Crowd Funding For Pirates"/>
    <meta name="keywords" content="max keiser, pirate film, market" />

    <title><%= @title ? "Pirate My Film - " + h(@title) : "Pirate My Film" %></title>

    <%= render :partial => 'shared/head_load' %>

    <%= yield :head %>
  </head>

  <body onload="doInit();">

    <div id="site_wrapper">

      <%= render :partial => 'shared/header_links' %>

      <div class="pmf_clear">&nbsp;</div>

      <div id="content_container">
        <div id="stage">
          <%= render :partial => 'shared/header' %>

          <div id="content">

            <div id="flashes">
              <%= show_default_flash -%>
            </div>

            <%= render :partial => "/blogs/subnav" %>

            <!--div id="profile_links">
              <% if logged_in? %>
                <div class="link">
                  <%= link_to 'My Profile', profile_path(@p) %>
                </div>
                <div class="link">
                  &nbsp;-&nbsp;
                  <%= link_to 'My Portfolio', portfolio_profile_path(@p) %>
                </div>
              <% end %>
            </div-->

            <%= yield %>

          </div>

          <div class="pmf_clear">&nbsp;</div>
          <%= render :partial => 'shared/footer' %>
        </div>
      </div>
    </div>

    <div id="footer_scripts">
      <%= render :partial => 'shared/footer_scripts' %>
    </div>

    <!-- IC Metric Beacon Code -->
    <script src="http://cdn.investingchannel.com/ic_beacon.js" type="text/javascript"></script>
    <script type="text/javascript"> icBeacon('piratemyfilm'); </script>

  </body>
</html>